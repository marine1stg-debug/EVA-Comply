# DEPRECATED — not used at runtime.
#
# The active config is generated at container start from `nginx.conf.template`
# by `docker-entrypoint-eva.sh` (see ./Dockerfile). That template adds the
# env-driven HTTP Basic Auth gate, the no-index header, and the robots.txt route.
#
# Editing this file has no effect. Edit `nginx.conf.template` instead.
