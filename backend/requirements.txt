# Web framework
# fastapi >= 0.115 required to pull starlette >= 0.40 (CVE-2024-47874 multipart DoS);
# starlette floor raised again for CVE-2025-62727 (Range-header ReDoS, fixed 0.49.1).
fastapi==0.136.1
starlette>=0.49.1
uvicorn[standard]==0.30.1

# Database
sqlalchemy[asyncio]==2.0.30
asyncpg==0.29.0
alembic==1.13.1
psycopg2-binary==2.9.9

# Auth — pin bcrypt to 4.0.1 for passlib compatibility
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
# 3.4.0 fixes CVE-2024-33663 (algorithm confusion / signing with public key)
# and CVE-2024-33664 (JWE decompression DoS) — never downgrade below 3.4.
python-jose[cryptography]>=3.4.0
pyotp==2.9.0
# 0.0.18+ fixes CVE-2024-53981 (multipart boundary DoS); also covers CVE-2024-24762.
python-multipart>=0.0.18

# Cache / Queue
redis==5.0.4
celery==5.4.0

# Storage
boto3==1.34.131

# Reports
weasyprint==62.3
pydyf==0.10.0          # must match weasyprint 62.3 — newer pydyf breaks with "'super' object has no attribute 'transform'"
openpyxl==3.1.4
python-docx==1.1.2
# Renders policy .docx → HTML for in-app full preview (pure-Python, no system deps).
mammoth==1.8.0

# Email
sendgrid==6.11.0

# Stripe
stripe==10.1.0

# Utils
# Ranged (not exact) so pip can satisfy fastapi 0.136's own pydantic floor.
pydantic>=2.11,<3
# Required by pydantic EmailStr (used in app/api/auth.py) — without it the API
# crashes on startup with: "email-validator is not installed"
email-validator>=2.2.0
pydantic-settings>=2.9,<3
python-dotenv==1.0.1
httpx==0.27.0
# 11.3.0 fixes CVE-2025-48379 (DDS heap buffer overflow) + earlier 10.x CVEs.
pillow>=11.3.0
python-magic==0.4.27
aiofiles==23.2.1

# ── Transitive security floors ───────────────────────────────────────────────
# (not direct imports — these force patched versions of indirect dependencies)
h11>=0.16          # CVE-2025-43859 HTTP request smuggling (via uvicorn/httpx)
httpcore>=1.0.9    # first release compatible with h11>=0.16
requests>=2.32.4   # CVE-2024-35195 / CVE-2024-47081 (via sendgrid/boto3 tooling)
